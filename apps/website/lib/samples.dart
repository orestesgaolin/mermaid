/// Sample catalogue, shared with the demo app and tests via the
/// `mermaid_samples` package. Re-exported here so existing imports keep working.
library;

export 'package:mermaid_samples/mermaid_samples.dart';
